# Resume guide

Read this first when opening the project from a new shell or agent session.

## One-line summary
Two Rainmeter skins: **InternetMonitor** (ping-latency/bandwidth/packet-loss monitor for `8.8.8.8` with a connect/disconnect toggle) and **ServerStatus** (2-device LAN ping up/down badge). Both are plain `.ini` configs, no Lua.

## What exists today
- Single commit history (`551427c` "Initial public release of rainmeter-skins") — the skins themselves are complete and released, not mid-development.
- `AGENTS.md` and `ai-context/` (this file, `technical.md`, `todo.md`, `build-plan.md`) exist on disk but are **untracked in git** (`git status --porcelain` shows `?? AGENTS.md` / `?? ai-context/`). They were generated by the project-context scaffolding tooling and, until this session, still had generic placeholder content, not project-specific facts.
- This session replaced the placeholder content with real details derived from reading `README.md`, `InternetMonitor/InternetStatus.ini`, `InternetMonitor/InternetController.bat`, and `ServerStatus/ServerState.ini`.

## Recommendation
Once you've reviewed `AGENTS.md` and `ai-context/*.md` for accuracy, **commit them** (e.g. `chore: add project context docs`). They were intentionally left uncommitted for this session per instructions — no commit was made by the agent that wrote this.

## Next action
1. Read `AGENTS.md` and `ai-context/technical.md`.
2. If you plan to change skin behavior, read the specific `.ini` file directly — these docs summarize logic but the `.ini` is the source of truth.
3. Mark `ai-context/todo.md` items `[x]` with a one-line test result when done.
